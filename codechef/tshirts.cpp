#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;
/*
long long f(vector<vector<int> > tshirt, int index)
{	
	
}

int main(void)
{
	cout<<"tshirt";
	int T;
	cin>>T;
	while(T--)
	{	int N;
		cin>>N;
		vector<vector<int> >tshirts(N);
		for(int i=0; i<N; i++)
		{	char c=' ';
			int n;
			while(c!='\n')
			{	scanf("%d%c", &n, &c);
				tshirts[i].push_back(n);
			}
			sort(tshirts[i].begin(), tshirts[i].end());
		}
		cout<<f(tshirts, 0);
	}
	return 0;
}*/
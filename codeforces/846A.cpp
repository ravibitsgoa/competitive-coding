#include<iostream>
using namespace std;
/*int main()
{
	int n;
	cin>>n;
	int ini=0, ones=0, zeros=0;
	int arr[n];
	for(int i=0; i<n; i++)
		cin>>arr[i];
	for(int i=n-1; i>=0; i--)
	{
		int x=arr[i];
		if(x==1)
			ones++;
		else if(ones==0)
			ini++;
		else 
			zeros++;
	}
	cout<<ini+ max(ones, zeros);
}*/
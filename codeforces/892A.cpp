#include <iostream>
#include <algorithm>
using namespace std;
/*int main()
{
	cin.tie(nullptr);
	cout.tie(nullptr);
	ios_base::sync_with_stdio(false);
	
	int n;
	cin>>n;
	long long b[n];
	long long sum=0;
	for(int i=0; i<n; i++)
	{	long long a;
		cin >> a;
		sum+= a;
	}
	
	for(int i=0; i<n; i++)
		cin >> b[i];
	sort(b, b+n);
	
	if(sum <= b[n-1]+ b[n-2])
		cout << "YES";
	else
		cout << "NO";
	return 0;
 }
*/
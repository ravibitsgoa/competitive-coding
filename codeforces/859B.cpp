#include <iostream>
using namespace std;
/*int main()
{
	int n;
	cin>>n;
	for(int i=0; (i*i) <= n; i++)
	{	if(i*i == n)
		{	cout<<4*(i);
			break;
		}
		else if((i+1)*(i+1) -1 == n)
		{	cout<<4*(i+1);
			break;
		}
		else if(i*i +1 == n)
		{	cout<<4*i + 2;
			break;
		}
		else if((i+1)*(i+1) > n)
		{	//cout<<i<<endl<<i*(i+1)<<endl;
			if( i*(i+1) >= n)
				cout<<4*i +2;
			else
				cout<<4*i +4;
			break;
		}
	}
	return 0;
}*/
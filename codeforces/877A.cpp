#include <iostream>
#include <vector>
using namespace std;
/*int main()
{
	cin.tie(nullptr);
	cout.tie(nullptr);
	ios_base::sync_with_stdio(false);
	
	string s;
	cin>>s;
	vector <string> x ={"Danil", "Olya", "Slava", "Ann", "Nikita"};
	int count=0;
	for(string j: x)
	{	int a= s.find(j);
		if(a!=s.npos)
		{	a = s.find("Danil", a+1);
			if(a == s.npos)
				count++;
			else
				count+=2;
		}
	}
	if(count==1)
		cout<<"YES";
	else
		cout<<"NO";
	return 0;
}
*/